const transitionDelay = 1000; /*time taken to slide from one image to next in millisecond */
const holdDelay = 1000; /* time the frame is paused before moving to the next one */
const transitionSmoothness = 10; /* the smoothness of each transition in FTD - frame per transition delay units */


const indicatorColor = "#ff0000"; /* bottom indicator color */
const indicatorActiveColor = "#000000"; /* bottom indicator color when clicked or auto scrolled */
const indicatorSize = 5;/* diameter of the round indicator dots in px */
const indicatorSpacing = 10; /* spacing in between each indicator */
const indicatorBottomSpacing = 10; /* position from the bottom edge of the image in px */ 